import Link from "next/link";import {redirect} from "next/navigation";import {prisma} from "@/lib/db";import {getCurrentEmployer} from "@/lib/session";
export default async function EmployerDashboard(){const ctx=await getCurrentEmployer();if(!ctx)redirect("/login");const jobs=await prisma.job.findMany({where:{employerId:ctx.employer.id},include:{applications:{include:{student:{include:{user:true}}}},requiredSkills:{include:{skill:true}}},orderBy:{createdAt:"desc"}});const apps=jobs.flatMap(j=>j.applications.map(a=>({...a,job:j}))).sort((a,b)=>b.matchScore-a.matchScore);return <main className="page-shell"><span className="eyebrow">Employer portal</span><h1 className="page-title">{ctx.employer.company}</h1><p className="muted">Structured hiring intelligence with human decisions kept in control.</p><div className="grid-3" style={{marginTop:26}}><div className="card"><span className="muted">Open roles</span><div className="metric">{jobs.filter(j=>j.status==="open").length}</div></div><div className="card"><span className="muted">Candidates</span><div className="metric">{apps.length}</div></div><div className="card"><span className="muted">Average match</span><div className="metric">{apps.length?Math.round(apps.reduce((n,a)=>n+a.matchScore,0)/apps.length):0}%</div></div></div><div className="grid-2" style={{marginTop:18,alignItems:"start"}}><section className="card"><div className="data-row"><h2>Roles</h2><Link className="button primary" href="/employer/jobs/new">Post a role</Link></div>{jobs.map(j=><div className="data-row" key={j.id}><div><strong>{j.title}</strong><div className="muted">{j.requiredSkills.map(s=>s.skill.name).join(", ")}</div></div><span className="pill">{j.applications.length} candidate(s)</span></div>)}</section><section className="card"><span className="eyebrow">AI candidate ranking</span><h2>Top candidates</h2>{apps.length?apps.map((a,i)=><div className="data-row" key={a.id}><div><strong>#{i+1} {a.student.user.name}</strong><div className="muted">{a.job.title}</div></div><span className="pill">{a.matchScore}% match</span></div>):<div className="notice">Applications will appear here with transparent match scores after students apply.</div>}</section></div></main>}
